name: Run Action
run-name: ${{ inputs.correlation_id }}
on: 
  workflow_call:
    inputs:
      api_inputs:
        description: Action info (json)
        type: string
        required: true
      config:
        description: Workflow config (json)
        type: string
        required: true
    secrets:
      secrets:
        description: Secrets info (json)
        required: true
env:
  STK: ${{ fromJson(inputs.config).stk }}
  DEBUG: ${{ fromJson(inputs.config).debug }}
jobs:

  validate-inputs:
    name: Validate JSON Inputs
    runs-on: ubuntu-latest
    steps:
      - name: Debug inputs
        if: env.DEBUG
        run: |
          echo "action: ${{ inputs.api_inputs }}"
      - name: "Validate Input: 'action.name'"
        if: fromJson(inputs.api_inputs).action_name == ''
        run: |
         echo "ERROR: 'name' field not found in json for 'action' input"
         exit 1
      - name: "Validate Input: 'cli_login.email'"
        if: fromJson(inputs.api_inputs).cli_login_email == ''
        run: |
         echo "ERROR: 'email' field not found in json for 'cli_login' input"
         exit 1
      - name: "Validate Input: 'cli_login.realm'"
        if: fromJson(inputs.api_inputs).cli_login_realm == ''
        run: |
         echo "ERROR: 'realm' field not found in json for 'cli_login' input"
         exit 1

  run-action-using-stk-cli:
    name: Run Stackspot Action
    needs: validate-inputs
    env:
      INPUTS: '${{ inputs.api_inputs }}'
    runs-on: ${{ fromJson(inputs.config).runner }}
    steps:
      - name: Checkout workflows repo
        uses: actions/checkout@v3
        with:
          repository: ${{ fromJson(inputs.config).origin_repository }}
          ref: ${{ fromJson(inputs.config).origin_branch }}

      - name: Install STK
        uses: ./github-actions/install-stk-cli
        with: 
          stk: ${{ fromJson(inputs.config).stk }}
      
      - name: Login STK
        uses: ./github-actions/login-stk
        with: 
          stk: ${{ fromJson(inputs.config).stk }}
          email: ${{ fromJson(inputs.api_inputs).cli_login_email }}
          pat: ${{ fromJson(secrets.secrets).cli_token }}
          realm: ${{ fromJson(inputs.api_inputs).cli_login_realm }}
      
      - name: Use workspace
        run: |
         $STK use workspace ${{ inputs.workspace_slug }}
      
      - name: Run action
        env:
          NAME: ${{ fromJson(inputs.api_inputs).action_name }}
          INPUTS: ${{ toJson(fromJson(inputs.api_inputs).action_inputs) }}
          CI: ${{ toJson(fromJson(inputs.api_inputs).action_connectors) }}
          ACTION_ENV: ${{ fromJson(inputs.api_inputs).action_env }}
        run: |
          source .github/workflows/scripts/enable_colors.sh
          conn=""
          if [ "${CI}" != "null" ]; then 
              conn=" --connection-interfaces $(echo $CI | jq . -c)"
          fi
          env=""

          if [ "${ACTION_ENV}" != "null" ]; then
            env=" --env $ACTION_ENV"
          fi

          if [ "${INPUTS}" != "null" ]; then 
              inputs_short=" --inputs-json $(echo $INPUTS | jq . -c)"
              echo $inputs_short
          fi

          echo -e "\n${green}⚡ Running Action: ${u_cyan}$NAME"
          echo -e "$STK run action $NAME $inputs_short $conn $env \n" 
          $STK run action $NAME $inputs_short $conn $env
      
      - name: Show Error Log
        if: failure()
        run: |
          cat ~/.$STK/logs/*